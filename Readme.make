
make

 configure and make cvs
 (server and client support are disabled)


make install
 
 install patchwork scripts and cvs

 default location: /usr/local/bin, /usr/local/share/doc/cvs
  (INSTALLPREF is set to /usr/local)

 change location: 
  make install INSTALLPREF=/usr 
 (e.g.)
	
